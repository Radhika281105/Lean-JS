const accountId = 14453
let accountEmail = "radhika@gmail.com"
var accountPassword = 212121
let accountCity = "Jaipur"

console.log([accountId,accountEmail,accountPassword,accountCity]);

console.table([accountId,accountEmail,accountPassword,accountCity]);

// prefer not to use "var" becoz of issue in block scope and functional scope

// if we declare a variable and don't define it then in output  we get "undefined"