documeny.getElementById is a method in JavaScript that allows you to access an HTML element by its unique ID. It returns the element object that has the specified ID, or null if no such element exists.

 document.getElementById('title')

 document.getElementById('title').getAttribute('class')

 document.getElementById('title').setAttribute('class','test')

document.getElementById('title').setAttribute('class','test heading')

store the value in a variable
const title = document.getElementById('title')
title.style.backgroundColor = 'green'
title.style.padding = "15px"
title.style.borderRadius = "15px"


//Imp
a. title.textContent -> displays that text also which is hidden or made invisible through css .
b. title.innerHTML -> it will also give the whole html coden if written inside the text.
c. title.innerText -> shows the text which is visible.

//in react
-> we use className insted of class to get the class name

//selects first (e.g.:-h1) from the whole page
//uses css-selectors
document.querySelector('h1')
document.querySelector('#title') //ID
document.querySelector('.heading') //class
document.querySelector(input[type = "password"])
document.querySelector('p:first-child')

selects all from the whole page

//gives a node list
document.querySelectorAll('li')

const tempLiList = document.querySelectorAll('li')
tempLiList[0].style.color = "green"

Node List and Html Collections are not purely array
becoz sometimes they are not provided with maps but forEach is present
but in array -> maps are always present and forEach also


//node list if used then -> then use "forEach" with it.

//to get HTML Collections
document.getElementByClassName('list-item') -> gives HTML Collections
//forEach doesn't work on HTML Collections

//to convert htl collection into array for iteration
const tempClassList = document.getElementByClassName('list-item')
Array.from(tempClassList)




